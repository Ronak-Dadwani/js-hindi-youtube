"use strict"; // test all JS code as newer version
// alert (3+3) we are using node.js , not browser

// code readability 

// for documentation the best mdn

console.log("Ronak")

let name = "ronak"
let age = 18;
let isLoggedIn = false;

// number => 2 to power 53 
//bigint => values represent numeric values which are too large to be represented by the number primitive

const alsoHuge = BigInt(9007199254740991);
console.log(alsoHuge);


//string => "" the string object is used represent and manipulate a sequence of characters.
//boolean =>  datatypes true/false
//null => standalone value  is not undefined. 
//undefined   => variable is been declared but value is not assigned.
//symbol => unique


//object 

console.log(typeof undefined);  // interview question typeof 

// null => object
// "ronak" => string
// undefined => undefined

